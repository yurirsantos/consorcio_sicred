export type LabelType = {
  title: string
  icon?: any
  required?: boolean
  textInfo?: string
}

export type TitleType = {
  title: string
  extraClass?: string
}

export type TextErrorType = {
  title: string
}

export type TitleLineType = {
  title: string
  extraClass?: string
  icon?: any
  onClickIcon?: any
}